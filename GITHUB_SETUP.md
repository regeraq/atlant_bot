# 📦 Инструкция по загрузке проекта на GitHub

Этот документ поможет вам загрузить проект на GitHub и настроить его для деплоя.

## 🔧 Шаг 1: Создание репозитория на GitHub

1. Войдите в свой аккаунт GitHub
2. Нажмите кнопку **"New"** (или перейдите по ссылке https://github.com/new)
3. Заполните форму:
   - **Repository name**: `atlant_bot` (или любое другое имя)
   - **Description**: "Telegram бот для управления арендой автомобилей"
   - Выберите **Private** (рекомендуется) или **Public**
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
4. Нажмите **"Create repository"**

## 📤 Шаг 2: Инициализация Git и загрузка кода

Откройте терминал в директории проекта и выполните:

```bash
# Проверяем, что мы в правильной директории
cd atlant_bot

# Инициализируем Git (если еще не инициализирован)
git init

# Добавляем все файлы
git add .

# Создаем первый коммит
git commit -m "Initial commit: Telegram bot for car rental management"

# Добавляем удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/atlant_bot.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/atlant_bot.git

# Загружаем код на GitHub
git branch -M main
git push -u origin main
```

## 🔐 Шаг 3: Настройка секретов (Secrets) для GitHub Actions (опционально)

Если вы планируете использовать GitHub Actions для автоматического деплоя:

1. Перейдите в ваш репозиторий на GitHub
2. Нажмите **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте следующие секреты:
   - `BOT_TOKEN` - токен вашего бота
   - `SERVER_HOST` - IP адрес или домен вашего сервера
   - `SERVER_USER` - имя пользователя для SSH
   - `SERVER_SSH_KEY` - приватный SSH ключ для доступа к серверу

## 🚀 Шаг 4: Деплой на сервер

После загрузки кода на GitHub, на вашем сервере выполните:

```bash
# Клонируем репозиторий
git clone https://github.com/YOUR_USERNAME/atlant_bot.git
cd atlant_bot

# Копируем пример файла окружения
cp env.example .env

# Редактируем .env файл
nano .env
# Заполните BOT_TOKEN, ADMIN_IDS и другие переменные

# Запускаем деплой
chmod +x deploy.sh
./deploy.sh
```

## 🔄 Обновление кода на сервере

Когда вы обновите код на GitHub, на сервере выполните:

```bash
cd atlant_bot
git pull
docker-compose down
docker-compose up -d --build
```

## 📝 Рекомендации

1. **Никогда не коммитьте `.env` файл** - он уже в `.gitignore`
2. **Используйте осмысленные сообщения коммитов**:
   ```bash
   git commit -m "Add: новая функция управления арендой"
   git commit -m "Fix: исправлена ошибка в обработке платежей"
   ```
3. **Создавайте ветки для новых функций**:
   ```bash
   git checkout -b feature/new-feature
   # ... делайте изменения ...
   git commit -m "Add: новая функция"
   git push origin feature/new-feature
   # Создайте Pull Request на GitHub
   ```
4. **Регулярно делайте коммиты** - не накапливайте изменения

## 🔒 Безопасность

- ✅ `.env` файл уже в `.gitignore` - он не будет загружен
- ✅ База данных (`*.db`) также игнорируется
- ✅ Логи и временные файлы не попадут в репозиторий
- ⚠️ **Проверьте перед коммитом**: `git status` - убедитесь, что нет чувствительных данных

## 📋 Чеклист перед первым коммитом

- [ ] Проверено, что `.env` файл не добавлен в Git
- [ ] Проверено, что база данных не добавлена в Git
- [ ] Все зависимости указаны в `requirements.txt`
- [ ] README.md обновлен и содержит актуальную информацию
- [ ] Код протестирован локально
- [ ] Создан репозиторий на GitHub
- [ ] Настроены переменные окружения на сервере

## 🆘 Решение проблем

### Ошибка: "remote origin already exists"

```bash
# Удалите старый remote
git remote remove origin
# Добавьте новый
git remote add origin https://github.com/YOUR_USERNAME/atlant_bot.git
```

### Ошибка при push: "Permission denied"

1. Проверьте, что вы авторизованы в GitHub
2. Используйте Personal Access Token вместо пароля
3. Или настройте SSH ключи

### Как получить Personal Access Token

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите права: `repo` (полный доступ к репозиториям)
4. Скопируйте токен и используйте его как пароль при push

## 📞 Дополнительная помощь

- [Документация GitHub](https://docs.github.com/)
- [Git Handbook](https://guides.github.com/introduction/git-handbook/)
- [GitHub CLI](https://cli.github.com/) - альтернативный способ работы с GitHub

