# 🤖 ATLANT BOT - Telegram Bot для аренды автомобилей

Telegram-бот для управления автопарком и арендой автомобилей с правом выкупа.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка окружения

Создайте файл `.env` на основе `env.example`:

```bash
cp env.example .env
```

Отредактируйте `.env` и укажите:
- `BOT_TOKEN` - токен вашего бота от @BotFather
- `ADMIN_IDS` - ID администраторов (через запятую)
- `FIRST_ADMIN_ID` - ваш Telegram ID (для первого администратора)
- `BOOKING_CONTACT_ID` - ID контакта для бронирования (опционально)

### 3. Запуск бота

#### Вариант A: С Docker (Рекомендуется)

```bash
# Создайте директории
mkdir -p data logs

# Запустите бота
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

Или используйте скрипт деплоя:

```bash
chmod +x deploy.sh
./deploy.sh
```

#### Вариант B: Без Docker

```bash
# Используйте скрипт запуска
chmod +x start.sh
./start.sh
```

Или вручную:

```bash
# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Запустите бота
python bot/main.py
```

## 📁 Структура проекта

```
atlant_bot/
├── bot/
│   ├── core/              # Ядро приложения
│   │   ├── bot.py        # Инициализация бота
│   │   └── config.py     # Конфигурация
│   ├── handlers/         # Обработчики сообщений
│   │   ├── admin/        # Административные handlers (модульная структура)
│   │   ├── user_handlers.py
│   │   ├── broadcast_handlers.py
│   │   └── contact_handlers.py
│   ├── database/         # Работа с БД
│   │   ├── database.py   # CRUD операции
│   │   ├── db_pool.py    # Пул соединений
│   │   ├── models.py     # SQL схемы
│   │   └── repositories/ # Repository Pattern (в разработке)
│   ├── services/         # Бизнес-логика (в разработке)
│   ├── middleware/       # Middleware для обработки запросов
│   ├── keyboards/        # Клавиатуры интерфейса
│   ├── utils/           # Вспомогательные функции
│   │   ├── cache.py     # Кэширование
│   │   ├── scheduler.py # Планировщик напоминаний
│   │   └── notifications.py # Система рассылок
│   ├── config.py        # Конфигурация (legacy, использует core/config.py)
│   └── main.py          # Точка входа
├── .env.example         # Пример переменных окружения
├── .gitignore          # Игнорируемые файлы
├── requirements.txt    # Зависимости Python
└── README.md           # Этот файл
```

## ✨ Основные функции

### Для пользователей:
- 🚗 Просмотр каталога автомобилей
- 📋 Детальная информация об автомобилях
- 📞 Связь с менеджером
- 👤 Профиль пользователя

### Для администраторов:
- 🚗 Управление автопарком (добавление, редактирование, удаление)
- 📝 Управление арендой
- 📢 Рассылка сообщений пользователям
- 📊 Статистика и аналитика
- 👥 Управление администраторами
- 📞 Управление контактами
- 💾 Экспорт базы данных

## 🔧 Технологии

- **Python 3.8+**
- **aiogram 3.x** - фреймворк для Telegram ботов
- **aiosqlite** - асинхронная работа с SQLite
- **python-dotenv** - управление переменными окружения

## 📝 Разработка

### Архитектурные улучшения

Проект находится в процессе рефакторинга. См. `АРХИТЕКТУРНЫЙ_АНАЛИЗ.md` и `РЕФАКТОРИНГ_ИНСТРУКЦИИ.md` для деталей.

### Выполненные улучшения:
- ✅ Безопасность: удален захардкоженный токен
- ✅ Управление зависимостями: создан requirements.txt
- ✅ Модульная структура: создана структура admin handlers
- ✅ Конфигурация: улучшена система конфигурации

### В разработке:
- 🔄 Repository Pattern
- 🔄 Service Layer
- 🔄 Валидация данных (Pydantic)
- 🔄 Улучшенное логирование
- 🔄 Тестирование

## 🚀 Деплой на сервер

Подробные инструкции по развертыванию бота на сервере см. в [DEPLOY.md](DEPLOY.md).

### Быстрый деплой с Docker:

```bash
# На сервере
git clone <ваш_репозиторий_url> atlant_bot
cd atlant_bot
cp env.example .env
nano .env  # Заполните переменные
chmod +x deploy.sh
./deploy.sh
```

## 📚 Документация

- `DEPLOY.md` - подробная инструкция по деплою на сервер
- `АРХИТЕКТУРНЫЙ_АНАЛИЗ.md` - полный архитектурный анализ проекта
- `РЕФАКТОРИНГ_ИНСТРУКЦИИ.md` - инструкции по завершению рефакторинга
- `ЗАПУСК_БОТА.md` - инструкция по запуску
- `АУДИТ_ПРОЕКТА.md` - аудит проекта

## 🔒 Безопасность

- ⚠️ **НИКОГДА** не коммитьте файл `.env` в репозиторий
- ⚠️ Используйте переменные окружения для чувствительных данных
- ⚠️ Регулярно обновляйте зависимости

## 📄 Лицензия

Проект для внутреннего использования.

## 👥 Контакты

Для вопросов и предложений обращайтесь к администратору проекта.






