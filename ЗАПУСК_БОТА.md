# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ БОТА

## ✅ Ваш ID администратора уже добавлен: `1043884207`

---

## 📋 ШАГИ ДЛЯ ЗАПУСКА

### 1️⃣ Активация виртуального окружения

**Windows (PowerShell):**
```powershell
.\.venv\Scripts\Activate.ps1
```

**Windows (CMD):**
```cmd
.\.venv\Scripts\activate.bat
```

**Linux/Mac:**
```bash
source .venv/bin/activate
```

### 2️⃣ Установка зависимостей (если еще не установлены)

```bash
pip install -r requirements.txt
```

**Примечание:** Проект использует собственный планировщик на `asyncio`, поэтому `apscheduler` не требуется.

### 3️⃣ Запуск бота

**Windows (после активации venv):**
```powershell
python bot/main.py
```

**Windows (без активации venv, используя Python из venv):**
```powershell
.\.venv\Scripts\python.exe bot/main.py
```

**Linux/Mac:**
```bash
python3 bot/main.py
```

---

## 🎯 АЛЬТЕРНАТИВНЫЙ СПОСОБ (если есть requirements.txt)

```bash
pip install -r requirements.txt
python bot/main.py
```

---

## ⚙️ НАСТРОЙКИ

### Токен бота
Токен уже настроен в `bot/config.py`

### ID администратора
Ваш ID `1043884207` уже добавлен в конфигурацию

### ID для бронирования
Также настроен на ваш ID `1043884207`

---

## 🔍 ПРОВЕРКА РАБОТЫ

После запуска вы должны увидеть:
```
✅ Загружено 1 ID администратор(ов) из переменной окружения
✅ ID контакта для бронирования загружен
Бот запущен...
📱 Доступные функции:
  🚗 Каталог автомобилей
  💰 Просмотр цен и описаний
  📝 Бронирование (в разработке)
  🔧 Админ панель (для администраторов)
==================================================
```

---

## 🛑 ОСТАНОВКА БОТА

Нажмите `Ctrl + C` в терминале

---

## ❓ ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### Ошибка: "Python was not found"
- Убедитесь, что Python установлен
- Проверьте, что виртуальное окружение активировано

### Ошибка: "Module not found"
- Установите зависимости: `pip install aiogram aiosqlite apscheduler`

### Ошибка: "Token is invalid"
- Проверьте токен бота в `bot/config.py`

---

## 📞 ПОЛУЧЕНИЕ СВОЕГО TELEGRAM ID

Если нужно узнать ID другого пользователя:
1. Напишите боту @userinfobot
2. Он покажет ваш Telegram ID

---

## ✨ ГОТОВО!

Теперь бот должен работать! Откройте Telegram и найдите вашего бота, отправьте команду `/start`

