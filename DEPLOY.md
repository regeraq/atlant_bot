# 🚀 Инструкция по деплою бота на сервер

Этот документ содержит подробные инструкции по развертыванию бота на вашем сервере.

## 📋 Требования

- Сервер с Linux (Ubuntu 20.04+ рекомендуется)
- Python 3.8+ или Docker
- Git (для клонирования репозитория)
- Доступ к серверу по SSH

## 🔧 Вариант 1: Деплой с Docker (Рекомендуется)

### Шаг 1: Подготовка сервера

```bash
# Обновляем систему
sudo apt update && sudo apt upgrade -y

# Устанавливаем Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Устанавливаем docker-compose
sudo apt install docker-compose -y

# Добавляем пользователя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker $USER
# Выйдите и войдите снова для применения изменений
```

### Шаг 2: Клонирование репозитория

```bash
# Клонируем репозиторий
git clone <ваш_репозиторий_url> atlant_bot
cd atlant_bot
```

### Шаг 3: Настройка переменных окружения

```bash
# Копируем пример файла окружения
cp .env.example .env

# Редактируем .env файл
nano .env
```

Заполните следующие переменные:
- `BOT_TOKEN` - токен бота от @BotFather
- `ADMIN_IDS` - ваш Telegram ID (можно получить через @userinfobot)
- `FIRST_ADMIN_ID` - ваш Telegram ID (для первого администратора)

### Шаг 4: Запуск бота

```bash
# Делаем скрипт исполняемым
chmod +x deploy.sh

# Запускаем деплой
./deploy.sh
```

Или вручную:

```bash
# Создаем директории
mkdir -p data logs

# Собираем и запускаем контейнер
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f
```

### Управление ботом

```bash
# Остановить бота
docker-compose stop

# Запустить бота
docker-compose start

# Перезапустить бота
docker-compose restart

# Просмотр логов
docker-compose logs -f

# Остановить и удалить контейнер
docker-compose down
```

## 🔧 Вариант 2: Деплой без Docker

### Шаг 1: Подготовка сервера

```bash
# Обновляем систему
sudo apt update && sudo apt upgrade -y

# Устанавливаем Python и pip
sudo apt install python3 python3-pip python3-venv -y
```

### Шаг 2: Клонирование репозитория

```bash
git clone <ваш_репозиторий_url> atlant_bot
cd atlant_bot
```

### Шаг 3: Настройка окружения

```bash
# Копируем пример файла окружения
cp .env.example .env

# Редактируем .env файл
nano .env
```

### Шаг 4: Установка зависимостей и запуск

```bash
# Делаем скрипт исполняемым
chmod +x start.sh

# Запускаем бота
./start.sh
```

Или вручную:

```bash
# Создаем виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt

# Создаем директории
mkdir -p data logs

# Запускаем бота
python bot/main.py
```

### Запуск в фоновом режиме (с systemd)

Создайте файл `/etc/systemd/system/atlant-bot.service`:

```ini
[Unit]
Description=Atlant Telegram Bot
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/path/to/atlant_bot
Environment="PATH=/path/to/atlant_bot/venv/bin"
ExecStart=/path/to/atlant_bot/venv/bin/python /path/to/atlant_bot/bot/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активируйте сервис:

```bash
sudo systemctl daemon-reload
sudo systemctl enable atlant-bot
sudo systemctl start atlant-bot

# Просмотр статуса
sudo systemctl status atlant-bot

# Просмотр логов
sudo journalctl -u atlant-bot -f
```

## 🔄 Обновление бота

### С Docker:

```bash
cd atlant_bot
git pull
docker-compose down
docker-compose up -d --build
```

### Без Docker:

```bash
cd atlant_bot
git pull
source venv/bin/activate
pip install -r requirements.txt
# Перезапустите бота (через systemd или вручную)
```

## 📊 Мониторинг

### Проверка работы бота

```bash
# С Docker
docker-compose ps
docker-compose logs --tail=50

# Без Docker (с systemd)
sudo systemctl status atlant-bot
sudo journalctl -u atlant-bot --tail=50
```

### Резервное копирование базы данных

```bash
# С Docker
docker-compose exec bot cp /app/data/bot_database.db /app/data/backup_$(date +%Y%m%d_%H%M%S).db

# Без Docker
cp data/bot_database.db data/backup_$(date +%Y%m%d_%H%M%S).db
```

## 🔒 Безопасность

1. **Никогда не коммитьте `.env` файл в Git**
2. **Используйте сильные пароли для SSH**
3. **Настройте firewall** (откройте только необходимые порты)
4. **Регулярно обновляйте систему и зависимости**
5. **Используйте HTTPS для веб-интерфейсов** (если есть)

## 🐛 Решение проблем

### Бот не запускается

1. Проверьте логи: `docker-compose logs` или `journalctl -u atlant-bot`
2. Убедитесь, что `.env` файл заполнен правильно
3. Проверьте, что токен бота валидный
4. Убедитесь, что порты не заняты

### Ошибки базы данных

1. Проверьте права доступа к файлу базы данных
2. Убедитесь, что директория `data` существует
3. Проверьте логи на наличие ошибок SQL

### Проблемы с Docker

```bash
# Пересоздать контейнер
docker-compose down
docker-compose up -d --build

# Очистить все (осторожно!)
docker-compose down -v
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь, что все переменные окружения установлены
3. Проверьте документацию aiogram
4. Создайте issue в репозитории

