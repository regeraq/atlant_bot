# 📋 Что нужно для деплоя бота на сервер

## ✅ Что я уже сделал

Я подготовил весь проект для деплоя:

1. ✅ **Dockerfile** - для контейнеризации бота
2. ✅ **docker-compose.yml** - для удобного управления контейнером
3. ✅ **env.example** - пример файла с переменными окружения
4. ✅ **deploy.sh** - скрипт автоматического деплоя
5. ✅ **start.sh** - скрипт запуска без Docker
6. ✅ **DEPLOY.md** - подробная инструкция по деплою
7. ✅ **GITHUB_SETUP.md** - инструкция по загрузке на GitHub
8. ✅ Обновлен **README.md** с информацией о деплое
9. ✅ Обновлен **.gitignore** для безопасности

## 🔑 Что нужно вам

### 1. Токен бота от @BotFather

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot` или `/token`
3. Следуйте инструкциям для создания нового бота или получения токена существующего
4. Скопируйте токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Ваш Telegram ID

1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Бот покажет ваш ID (например: `123456789`)
4. Запишите этот ID

### 3. Сервер для деплоя

Вам нужен сервер с:
- **Linux** (Ubuntu 20.04+ рекомендуется)
- **Доступ по SSH**
- **Docker** (или Python 3.8+ если без Docker)

Варианты серверов:
- VPS (DigitalOcean, Linode, Vultr, Timeweb и т.д.)
- Облачный сервер (AWS, Google Cloud, Azure)
- Ваш собственный сервер

### 4. Доступ к GitHub (опционально, но рекомендуется)

- Аккаунт на GitHub
- Репозиторий для хранения кода

## 📝 Следующие шаги

### Вариант 1: Деплой с GitHub (Рекомендуется)

1. **Загрузите код на GitHub:**
   - Следуйте инструкциям в `GITHUB_SETUP.md`
   - Или создайте репозиторий вручную и загрузите файлы

2. **На сервере:**
   ```bash
   git clone <ваш_репозиторий_url> atlant_bot
   cd atlant_bot
   cp env.example .env
   nano .env  # Заполните переменные
   chmod +x deploy.sh
   ./deploy.sh
   ```

### Вариант 2: Деплой без GitHub

1. **Загрузите файлы на сервер:**
   - Через SCP, SFTP или любой другой способ
   - Или создайте архив и загрузите его

2. **На сервере:**
   ```bash
   cd atlant_bot
   cp env.example .env
   nano .env  # Заполните переменные
   chmod +x deploy.sh
   ./deploy.sh
   ```

## 🔧 Настройка .env файла

Создайте файл `.env` на основе `env.example` и заполните:

```env
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=ваш_telegram_id
FIRST_ADMIN_ID=ваш_telegram_id
DB_PATH=/app/data/bot_database.db
LOG_LEVEL=INFO
NOTIFICATION_TIME=10:00
```

## 📚 Документация

- **DEPLOY.md** - подробная инструкция по деплою
- **GITHUB_SETUP.md** - как загрузить проект на GitHub
- **README.md** - общая информация о проекте

## 🆘 Если что-то не работает

1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь, что все переменные в `.env` заполнены
3. Проверьте, что токен бота валидный
4. Убедитесь, что сервер имеет доступ в интернет
5. Проверьте инструкции в `DEPLOY.md`

## 💡 Полезные команды

```bash
# Просмотр логов
docker-compose logs -f

# Остановить бота
docker-compose stop

# Запустить бота
docker-compose start

# Перезапустить бота
docker-compose restart

# Обновить бота (после git pull)
docker-compose down
docker-compose up -d --build
```

## ✅ Чеклист перед деплоем

- [ ] Есть токен бота от @BotFather
- [ ] Знаю свой Telegram ID
- [ ] Есть сервер с доступом по SSH
- [ ] На сервере установлен Docker (или Python 3.8+)
- [ ] Создан файл `.env` с правильными значениями
- [ ] Прочитал инструкцию в `DEPLOY.md`

---

**Готово к деплою!** 🚀

Если у вас есть вопросы или нужна помощь - дайте знать!

