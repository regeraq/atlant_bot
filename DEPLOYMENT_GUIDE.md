# 🚀 Гайд по установке бота на сервер

Полная инструкция по развертыванию Telegram бота на сервере с рекомендациями по характеристикам для высокой нагрузки.

## 📋 Требования к серверу

### Минимальные характеристики (для тестирования)

- **CPU**: 1 ядро
- **RAM**: 512 MB
- **Диск**: 5 GB
- **ОС**: Ubuntu 20.04+ / Debian 11+
- **Сеть**: Стабильное интернет-соединение

### Рекомендуемые характеристики (для продакшена)

- **CPU**: 2-4 ядра (2.0+ GHz)
- **RAM**: 2-4 GB
- **Диск**: 20 GB SSD
- **ОС**: Ubuntu 22.04 LTS
- **Сеть**: 100+ Mbps

### Характеристики для высокой нагрузки

Для бота с **большим количеством пользователей** (1000+ активных, 10,000+ сообщений/день):

#### Вариант 1: Средняя нагрузка (1,000-5,000 пользователей)
- **CPU**: 4 ядра (2.4+ GHz)
- **RAM**: 4-8 GB
- **Диск**: 50 GB SSD
- **Сеть**: 1 Gbps
- **Оценка**: ~$20-40/месяц (VPS)

#### Вариант 2: Высокая нагрузка (5,000-20,000 пользователей)
- **CPU**: 8 ядер (2.6+ GHz)
- **RAM**: 16 GB
- **Диск**: 100 GB SSD (NVMe предпочтительно)
- **Сеть**: 1 Gbps
- **Оценка**: ~$80-150/месяц (VPS)

#### Вариант 3: Очень высокая нагрузка (20,000+ пользователей)
- **CPU**: 16+ ядер (3.0+ GHz)
- **RAM**: 32+ GB
- **Диск**: 200+ GB NVMe SSD
- **Сеть**: 10 Gbps
- **База данных**: Рекомендуется PostgreSQL вместо SQLite
- **Оценка**: ~$300+/месяц (VPS) или выделенный сервер

### Оптимизация производительности

1. **Используйте SSD** вместо HDD для базы данных
2. **Настройте пул соединений** к базе данных
3. **Включите кэширование** для часто запрашиваемых данных
4. **Используйте асинхронные операции** (уже реализовано)
5. **Настройте rate limiting** для защиты от перегрузки
6. **Мониторинг ресурсов**: используйте `htop`, `iotop`, `netstat`

### Расчет нагрузки

Примерный расчет ресурсов:
- **1 пользователь** ≈ 0.5-1 MB RAM
- **1000 активных пользователей** ≈ 500 MB - 1 GB RAM
- **10,000 сообщений/день** ≈ минимальная нагрузка на CPU
- **База данных SQLite**: до 10,000 пользователей комфортно
- **База данных PostgreSQL**: для 10,000+ пользователей

## 🔧 Установка на сервер

### Шаг 1: Подготовка сервера

```bash
# Обновляем систему
sudo apt update && sudo apt upgrade -y

# Устанавливаем необходимые пакеты
sudo apt install -y git curl wget nano htop

# Устанавливаем Docker (рекомендуется)
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Устанавливаем docker-compose
sudo apt install docker-compose -y

# Добавляем пользователя в группу docker
sudo usermod -aG docker $USER
# Выйдите и войдите снова для применения изменений
exit
```

### Шаг 2: Клонирование репозитория

```bash
# Клонируем репозиторий
cd ~
git clone https://github.com/regeraq/atlant_bot.git
cd atlant_bot
```

### Шаг 3: Настройка переменных окружения

```bash
# Копируем пример файла
cp env.example .env

# Редактируем .env файл
nano .env
```

**Обязательные переменные:**
```env
BOT_TOKEN=ваш_токен_от_BotFather
FIRST_ADMIN_ID=ваш_telegram_id
```

**Как получить:**
- **BOT_TOKEN**: Откройте [@BotFather](https://t.me/BotFather) в Telegram → `/newbot` или `/token`
- **FIRST_ADMIN_ID**: Откройте [@userinfobot](https://t.me/userinfobot) → `/start`

**Опциональные переменные для оптимизации:**
```env
# Для высокой нагрузки
BROADCAST_BATCH_SIZE=50          # Размер батча для рассылки (по умолчанию 30)
BROADCAST_BATCH_PAUSE_SMALL=0.1  # Пауза между батчами (секунды)
LOG_LEVEL=INFO                   # Уровень логирования (DEBUG/INFO/WARNING/ERROR)
```

### Шаг 4: Запуск бота

```bash
# Создаем необходимые директории
mkdir -p data logs

# Запускаем через Docker (рекомендуется)
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f
```

### Альтернатива: Запуск без Docker

```bash
# Устанавливаем Python 3.8+
sudo apt install python3 python3-pip python3-venv -y

# Создаем виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt

# Запускаем бота
python bot/main.py
```

## 🔄 Управление ботом

### Основные команды

```bash
# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f

# Остановить бота
docker-compose stop

# Запустить бота
docker-compose start

# Перезапустить бота
docker-compose restart

# Обновить бота (после изменений в GitHub)
cd ~/atlant_bot
git pull
docker-compose down
docker-compose up -d --build
```

### Настройка автозапуска (systemd)

Создайте файл `/etc/systemd/system/atlant-bot.service`:

```bash
sudo nano /etc/systemd/system/atlant-bot.service
```

Вставьте следующее (замените `your_username` на ваше имя пользователя):

```ini
[Unit]
Description=Atlant Telegram Bot
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/home/your_username/atlant_bot
ExecStart=/usr/bin/docker-compose up -d
ExecStop=/usr/bin/docker-compose down
User=your_username
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активируйте сервис:

```bash
sudo systemctl daemon-reload
sudo systemctl enable atlant-bot
sudo systemctl start atlant-bot

# Проверка статуса
sudo systemctl status atlant-bot
```

## 📊 Мониторинг и оптимизация

### Мониторинг ресурсов

```bash
# Мониторинг CPU и RAM
htop

# Мониторинг диска
df -h
du -sh ~/atlant_bot/data/*

# Мониторинг сети
iftop

# Мониторинг процессов
ps aux | grep python
```

### Оптимизация для высокой нагрузки

1. **Увеличение размера батчей рассылки:**
   ```env
   BROADCAST_BATCH_SIZE=100
   BROADCAST_BATCH_PAUSE_SMALL=0.05
   ```

2. **Оптимизация базы данных:**
   ```bash
   # Для SQLite (если используется)
   sqlite3 data/bot_database.db "PRAGMA journal_mode=WAL;"
   sqlite3 data/bot_database.db "PRAGMA synchronous=NORMAL;"
   ```

3. **Ограничение логирования:**
   ```env
   LOG_LEVEL=WARNING  # Вместо INFO для снижения нагрузки на диск
   ```

4. **Регулярная очистка логов:**
   ```bash
   # Добавьте в crontab
   crontab -e
   # Добавьте строку:
   0 0 * * 0 find ~/atlant_bot/logs -name "*.log" -mtime +7 -delete
   ```

### Резервное копирование

```bash
# Создание резервной копии базы данных
cd ~/atlant_bot
cp data/bot_database.db data/backup_$(date +%Y%m%d_%H%M%S).db

# Автоматическое резервное копирование (добавьте в crontab)
# Каждый день в 3:00
0 3 * * * cd ~/atlant_bot && cp data/bot_database.db data/backup_$(date +\%Y\%m\%d).db && find data/backup_*.db -mtime +30 -delete
```

## 🔒 Безопасность

1. **Настройте firewall:**
   ```bash
   sudo ufw allow 22/tcp  # SSH
   sudo ufw enable
   ```

2. **Используйте SSH ключи** вместо паролей

3. **Регулярно обновляйте систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

4. **Не коммитьте `.env` файл** в Git

5. **Используйте сильные пароли** для всех сервисов

## 🐛 Решение проблем

### Бот не запускается

```bash
# Проверьте логи
docker-compose logs -f

# Проверьте .env файл
cat .env

# Пересоздайте контейнер
docker-compose down
docker-compose up -d --build
```

### Высокая нагрузка на CPU/RAM

1. Проверьте количество активных пользователей
2. Увеличьте размер батчей рассылки
3. Рассмотрите миграцию на PostgreSQL
4. Увеличьте ресурсы сервера

### Проблемы с базой данных

```bash
# Проверьте права доступа
ls -la data/
chmod 755 data/
chmod 644 data/*.db

# Проверьте размер базы данных
du -sh data/bot_database.db
```

### Ошибка "Permission denied" с Docker

```bash
sudo usermod -aG docker $USER
# Выйдите и войдите снова
```

## 📈 Масштабирование

### Горизонтальное масштабирование

Для очень высокой нагрузки рассмотрите:
1. **Несколько инстансов бота** с балансировкой
2. **Отдельный сервер для базы данных** (PostgreSQL)
3. **Redis** для кэширования и очередей
4. **Load balancer** для распределения нагрузки

### Вертикальное масштабирование

Просто увеличьте ресурсы сервера:
- Больше CPU ядер
- Больше RAM
- Более быстрый диск (NVMe SSD)

## ✅ Чеклист после установки

- [ ] Сервер настроен с достаточными ресурсами
- [ ] Docker установлен и работает
- [ ] Репозиторий клонирован
- [ ] `.env` файл создан и заполнен
- [ ] Бот запущен (`docker-compose ps` показывает "Up")
- [ ] Логи показывают успешный запуск
- [ ] Бот отвечает в Telegram на `/start`
- [ ] Админ-панель доступна (если вы админ)
- [ ] Настроен автозапуск через systemd
- [ ] Настроено резервное копирование
- [ ] Настроен мониторинг ресурсов

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs -f`
2. Проверьте ресурсы сервера: `htop`, `df -h`
3. Убедитесь, что все переменные окружения установлены
4. Проверьте документацию aiogram: https://docs.aiogram.dev/

---

**Готово!** 🎉 Ваш бот должен работать на сервере с оптимальной производительностью.

